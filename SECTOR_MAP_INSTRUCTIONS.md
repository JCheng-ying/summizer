# 板块底图刷新说明(给定期刷新六大板块数据的 Claude 看)

这份文档说明如何刷新知识图谱里"六大板块的热门/冷门股票 + 供应链/竞争关系"这一层底图数据。
和 `PIPELINE_INSTRUCTIONS.md`(逐篇文章驱动的图谱更新)是两套独立机制——那份文档处理的是
"这篇新文章提到了哪些实体关系",这份文档处理的是"整个板块本身的公司地图长什么样",两者共用
同一个 `data/graph.json`,但刷新节奏不同。

## 六大板块

机器人板块、AI板块、能源板块、量子计算板块、太空经济板块、edge AI板块(定义见
`src/graph_store.py` 的 `CANONICAL_SECTORS`)。

## 刷新节奏

建议按季度财报节奏刷新(每个板块的热门公司大多是上市公司,财报季前后信息最新)。也可以在
用户明确要求时随时手动触发。

## 怎么做

对六个板块**各自独立**执行下面的流程(可以并行,比如分别开 6 个 Agent 子任务去做,互不依赖):

1. **今天的日期以实际系统时间为准**,不要用自己训练数据里的旧知识直接下结论——公司可能已
   经改名、被收购、退市、破产,股价/市值排名也可能早就变了。任何"这家公司现在怎么样"的判断
   都要用 WebSearch/WebFetch 实时查证。

2. 找出这个板块里 8-12 家"热门"公司(hot——市值大、交易活跃、媒体报道多、大众认知度高的
   上市公司,或虽未上市但极高知名度的公司)和 8-12 家"冷门"公司(cold——同产业链里不那么
   出名但确实在做事的中小盘上市公司、供应链零部件公司、或早期私人公司)。

3. 查证这些公司两两之间真实、可追溯来源的关系:合作/竞争/投资/收购/供应/客户/研究合作/母
   子公司,**重点是上下游供应链**(谁给谁供应什么产品或服务、谁是谁的客户)。信息来源优先用
   最近一年内的财报(10-K/10-Q/年报)、财报电话会议、官方新闻稿、可靠财经媒体报道。**每条
   关系必须有具体可访问的来源 URL**,查不到来源的关系不要写,不要编造。

4. 把结果整理成下面的 JSON,存到临时文件(比如 `/tmp/sector_<板块>.json`):

   ```json
   {
     "entities": [
       {"name": "公司英文名", "type": "company", "sector_tags": ["机器人板块"], "hot": "hot 或 cold", "ticker": "TSLA 或 null"}
     ],
     "relationships": [
       {
         "source": "公司A", "target": "公司B", "type": "supplier",
         "description": "一句中文说明这段关系具体是什么",
         "source_url": "https://...", "source_title": "来源标题",
         "source_name": "来源媒体/机构名", "published": "YYYY-MM-DD"
       }
     ]
   }
   ```

5. 需要一个占位的"文章"对象作为 `merge-graph` 的必填参数(实际证据来自每条 relationship
   自带的 `source_url` 等字段,这个占位对象只是走一下接口):

   ```json
   {"title": "<板块名>底图调研", "link": "", "source_name": "行业调研", "published": "<今天日期>"}
   ```
   存到 `/tmp/sector_item_<板块>.json`,然后:
   ```bash
   python -m src.main merge-graph --item-file /tmp/sector_item_<板块>.json --extraction-file /tmp/sector_<板块>.json
   ```

6. **六个板块的 merge-graph 必须按顺序依次执行,不要并行写**(`data/graph.json` 是单个文件,
   并行写会互相覆盖)。研究阶段可以并行(比如 6 个子任务同时跑 WebSearch),但最后落盘合并
   这一步要串行。

7. 全部合并完后重新生成可视化:
   ```bash
   python -m src.main graph
   ```

8. 如果是在云端定时任务里跑,按 `PIPELINE_INSTRUCTIONS.md` 里同样的方式把 `data/` 和 `web/`
   的变更 commit + push 回仓库。

## 注意事项

- 这一层是"板块底图",相对稳定,不需要每次都大改——如果某家公司的关系上次已经记录过、这次
  没查到更新,不用重复写(`merge-graph` 本身对同一 URL 的证据会自动去重,重复调用不会出错,
  但没必要浪费查证时间)。
- 热门/冷门的判断标准会随时间变化,刷新时可以调整已有公司的 `hot` 字段(比如原来是 cold 的
  公司现在涨成大盘股了),但改动之前也要有实际依据。
